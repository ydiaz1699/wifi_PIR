// ===== secrets.h.template - PLANTILLA DE CREDENCIALES =====
// 1. Copia este archivo como 'include/secrets.h'
// 2. Rellena tus credenciales WiFi
// 3. NUNCA subas 'secrets.h' a Git (está en .gitignore)
//
// Este archivo SÍ se sube a Git como referencia.

#pragma once
#include <array>

namespace secrets {
    inline constexpr std::array<char, 32> WIFI_SSID     = {"TU_WIFI_AQUI"};
    inline constexpr std::array<char, 64> WIFI_PASSWORD = {"TU_PASSWORD_AQUI"};

    // ===== Zona horaria =====
    // UTC-4  → Bolivia, Venezuela, Chile (no DST)  → -14400
    // UTC-3  → Argentina, Brasil (no DST)            → -10800
    // UTC-5  → Colombia, Perú, Ecuador               → -18000
    // UTC-6  → México (Centro)                       → -21600
    // UTC+0  → Reino Unido, Portugal               → 0
    // UTC+1  → España, Francia, Alemania           → 3600
    inline constexpr long  GMT_OFFSET_SEC      = -14400;  // Bolivia (UTC-4)
    inline constexpr int   DAYLIGHT_OFFSET_SEC = 0;

    inline constexpr std::array<char, 16> NTP_SERVER = {"pool.ntp.org"};
}
