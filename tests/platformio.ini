[platformio]
test_dir = test
src_dir = src

[env:native_test]
platform = native
test_framework = unity
test_build_src = yes
build_src_filter =
    +<*>
build_flags =
    -Ihost_compat
    -I../lib/IoTProtocol
